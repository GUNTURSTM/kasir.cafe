$kind: http-request
url: "{{base_url}}/api/transactions"
method: POST
headers:
  Authorization: Bearer {{cashier_token}}
  Content-Type: application/json
body:
  type: text
  content: |-
    {
        "items": [
            {
                "menu_id": 1,
                "quantity": 1
            }
        ],
        "payment_method": "cash",
        "paid_amount": 20000
    }
order: 1000
